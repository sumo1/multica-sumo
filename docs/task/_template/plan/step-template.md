# step-N: {子任务标题}

> 所属任务: {task-id} ｜ 依赖: {前置 step 或「无」} ｜ 并行组: {组号或「独立串行」}

## 施工契约（给执行 Agent）

### 范围
- 可改文件:
  - `path/to/file`
- 不可改文件 / 冻结边界:
  - `path/to/frozen`

### 产出清单
- {具体符号：函数 / 端点 / schema / 组件，带签名}

### 约束
- {已冻结的边界：响应结构、错误码、命名约定}
- {必须复用的现有模式，指向同模块已有实现}

### 复用参考
- `path/to/similar-impl` — {为什么参考它}

## 验收契约（给验收）

### 代码结构验证
- [ ] 文件 `X` 中新增 `Y`，签名 `Z`

### 命令验收
| 命令 | 通过标准 |
|------|---------|
| `pnpm typecheck` | 0 error |
| `{针对性测试}` | {可机器判定的通过条件} |

### 数据 / 字段验收
- [ ] {接口响应 / 类型包含字段 X，类型 Y}

### 负面用例
- [ ] {缺 X → 预期错误码 / 行为}
